# Generic video model key - works with OpenRouter, MiniMax, Google Gemini, Grok, etc.
# Copy your provider's API key here. Provider is selected in config/pipeline.yaml.
VIDEO_MODEL_KEY=

# MiniMax-specific: required only when provider is "minimax" (upload uses this for group auth)
MINIMAX_GROUP_ID=
